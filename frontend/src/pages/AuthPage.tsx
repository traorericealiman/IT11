// Re-export from the original location to maintain backward compatibility
// The actual implementation lives in src/login.tsx and will be cleaned up
// once the routing is updated.
export { default } from '../login';
